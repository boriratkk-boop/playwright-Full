{
    "name": "playwright-full",
    "version": "1.0.0",
    "description": "Playwright Full Framework - Senior QA Style",
    "scripts": {
      "test": "playwright test",
      "test:smoke": "playwright test --grep @smoke",
      "test:regression": "playwright test --grep @regression"
    },
    "devDependencies": {
      "@playwright/test": "^1.42.0"
    }
  }
  